services:
  api:
    build:
      context: .
      dockerfile: dockerfile
    container_name: uploaddataapi-dev
    ports:
      - "3000:3000"
    volumes:
      - .:/app
      - /app/node_modules
    env_file:
      - .env
    restart: unless-stopped